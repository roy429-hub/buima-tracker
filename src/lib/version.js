// Single source of truth for the version string shown in the UI.
// The footer, the login screen and the public share view had drifted apart
// (v0.1 vs v0.2); import this instead of hardcoding a new one.
export const APP_VERSION = "v0.2";
